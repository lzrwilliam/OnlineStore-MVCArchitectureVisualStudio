@using ProiectPAW.Models
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Utilizator

<div class="container">
    <h1 class="text-center">Login</h1>
    <div class="row">
        <div class="mx-auto w-50">
            <form id="account" asp-controller="Authentication" asp-action="Login" method="post">
                <div class="form-group">
                    <label asp-for="Nume" class="control-label">Username</label>
                    <input asp-for="Nume" class="form-control bg-light" />
                    <span asp-validation-for="Nume" class="text-danger"></span>
                </div>
               

                <div class="form-group">
                    <label asp-for="Parola" class="control-label">Password</label>
                    <input asp-for="Parola" class="form-control bg-light" />
                    <span asp-validation-for="Parola" class="text-danger"></span>
                </div>

                @if (!ViewData.ModelState.IsValid)
                {
                    if (ViewData.ModelState[string.Empty] != null)
                    {
                        <div class="error-summary">
                            @foreach (var error in ViewData.ModelState[string.Empty].Errors)
                            {
                                <div class="error-item text-danger">
                                    @error.ErrorMessage
                                </div>
                            }
                        </div>
                    }
                }
                <br />
                <button type="submit" class="btn btn-primary mt-3">Log in</button>
            </form>
        </div>
    </div>
</div>
